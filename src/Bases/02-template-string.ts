const firstName: string = 'Carlos';
const lastName: string = 'O\'Neil'; //Con el backslash se pueden dejar caracteres como estas comillas.

console.log(`
    el nombre es:
        ${firstName} ${lastName}`
)